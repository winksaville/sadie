machine:
  environment:
    INSTALL_PREFIX_DIR_HOME_REL: ~/opt
    INSTALL_PREFIX_DIR: ${HOME}/opt
    PATH: ${INSTALL_PREFIX_DIR}/bin:${INSTALL_PREFIX_DIR}/cross/bin:${PATH}

  #python:
  #  version: 3.5.0

  pre:
    # Needed by test.sh
    - sudo apt-get install realpath

    # Needed to compile ninja
    - sudo apt-get install re2c

    # Neadded to compile binutils-gdb
    - sudo apt-get install texinfo

    # Needed to compile gcc native compiler
    - sudo apt-get install libgmp-dev libmpfr-dev libmpc-dev

    # Needed to compile crosstool-ng
    - sudo apt-get install gperf help2man

    # We need gcc/g++ 5 because options -Wpednatic and -std
    # are not in older compilers and they generate unknown
    # command line option errors. The update-alternatives
    # makes the compilers the default.
    #- sudo apt-get update; sudo apt-get install gcc-5; sudo apt-get install g++-5
    #- sudo update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-5 100
    #- sudo update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-5 100

    # Try 4.9 to see if its upto date enough to compile our code.
    - sudo update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-4.9 100
    - sudo update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-4.9 100

checkout:
  post:
    # Update the submodules
    - git submodule sync --recursive
    - git submodule update --recursive --init

dependencies:
  cache_directories:
    #- ${INSTALL_PREFIX_DIR_HOME_REL} # This doesn't work.
    - ~/opt

  pre:
    # Default to python 3.5.0, needed because subprocess.run is used in vendor-install-tools
    - pyenv global 3.5.0

    # Install the tools we need
    - vendor-install-tools/install.py meson --installPrefixDir=${INSTALL_PREFIX_DIR}
    - vendor-install-tools/install.py ninja --installPrefixDir=${INSTALL_PREFIX_DIR}
    - vendor-install-tools/install.py ct-ng --installPrefixDir=${INSTALL_PREFIX_DIR}
    - vendor-install-tools/install.py gcc-x86_64 --installPrefixDir=${INSTALL_PREFIX_DIR}
    - vendor-install-tools/install.py gcc-i386 --installPrefixDir=${INSTALL_PREFIX_DIR}
    - vendor-install-tools/install.py gcc-arm-eabi --installPrefixDir=${INSTALL_PREFIX_DIR}
    - vendor-install-tools/install.py qemu-system-arm --installPrefixDir=${INSTALL_PREFIX_DIR}

test:
  override:
    - circleci-matrix/process-matrix platform-matrix.yml:
        parallel: true
